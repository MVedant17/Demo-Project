# Simple Interest Calculator

This project contains a Bash script named `simple-interest.sh` that calculates simple interest based on user input: principal, rate, and time.

## Usage
Run the script using:

bash simple-interest.sh
